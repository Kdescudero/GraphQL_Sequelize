type TypePreference {
  id: ID!
  description: String!
  # Asociación de uno a muchos
  preference: [Preference!]
}

type Query {
  allTypePreference: [TypePreference!]
}

type Mutation {
  newTypePreference(description: String!, preference: [Preferenceinput]): TypePreference!
}

input Preferenceinput {
  description: String!
}
